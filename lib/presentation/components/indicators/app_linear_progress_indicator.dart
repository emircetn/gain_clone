import 'package:flutter/material.dart';

class AppLinearProgressIndicator extends LinearProgressIndicator {
  const AppLinearProgressIndicator({Key? key})
      : super(
          key: key,
          color: Colors.red,
          backgroundColor: Colors.black,
        );
}
